# Site settings
title: "ML Course Project"
description: "Machine Learning Course Project - Academic Portfolio"
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
theme: minima

# Exclude custom layouts from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Sass settings
sass:
  style: compressed
